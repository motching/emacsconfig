#!/usr/bin/env bash
# Just so I remember the configure flags.
# To be run from the main emacs folder.
# For 29.0.90, later this stuff might become obsolete.
./configure --with-native-compilation --with-json --with-imagemagick --with-xwidgets
